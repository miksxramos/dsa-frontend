services:
  frontend:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
#     networks:
#       - mynetwork

# networks:
#   mynetwork:
#     driver: bridge


# volumes:
#   db_data: